# vertical_marketing/landing_config_builder

Construye un `landing_config.json` normalizado para un `template_type`.

Soporta inicialmente `property_sales`. Puede escribir el JSON a disco si se
pasa `output_path` y `dry_run=false`.
