# EMP_MULTI_SHOPPER

Empresa operativa interna para el dashboard de Purchasing IA Engine.

Reglas:
- La logica reusable vive en `factory/skills/internos/vertical_multi_shopper`.
- El dashboard operativo vive en `companies/EMP_MULTI_SHOPPER/projects/PROY-001_DASHBOARD`.
- La identidad vendible sale de `project.json`, `project-context.json` y variables de entorno.
- Antes de cierre correr `factory_no_hardcode_audit`.
