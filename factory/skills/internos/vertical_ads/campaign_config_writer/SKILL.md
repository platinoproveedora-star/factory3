# vertical_ads/campaign_config_writer

Actualiza un JSON de campana dentro de `companies/<COMPANY_ID>/`.

Campos tipicos: `landing_url`, `image_url`, `privacy_url`, `whatsapp_number`,
`approver`, `daily_budget`, `meta_ad_account_id`, `meta_page_id`, `form_id`.
