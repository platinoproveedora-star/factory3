{
  "type": "skill",
  "name": "vertical_factu4all/company_settings_manage",
  "version": "0.1.0",
  "kind": "executable",
  "entrypoint": "skill.py",
  "description": "CRUD de factu4all_company_settings (country, default_pac_provider, default_environment) por company_id. action=get|save (upsert). dry_run=true por defecto.",
  "requires_env": ["SUPABASE_URL", "SUPABASE_SERVICE_ROLE_KEY"]
}
