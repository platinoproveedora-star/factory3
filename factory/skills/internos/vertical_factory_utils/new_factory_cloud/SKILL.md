# new_factory_cloud

Publica una fabrica local limpia en cloud.

Valida una carpeta generada por `new_factory`, crea o usa un repo GitHub, sube archivos, crea un servicio Render y configura webhook Telegram.

Usar `dry_run: true` para validar plan sin tocar servicios externos.
